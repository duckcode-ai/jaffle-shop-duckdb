# Direct dependencies for local development.
#
# Keep this file lightly pinned so pip can resolve compatible transitive
# dependencies across supported Python versions, including Python 3.13.
#
# datalex-cli extras: [serve] bundles a portable Node runtime for the local UI;
# [duckdb] adds the DuckDB driver; [draft]+[draft-openai] install the Claude and
# OpenAI SDKs that power the AI Setup + Generate workflow (Ollama needs no extra).
# Gemini (draft-gemini) is omitted: its google-generativeai deps pull a protobuf
# version that conflicts with dbt-core. Pin >=1.12 so the bundled UI is current.

dbt-duckdb>=1.10,<2.0
datalex-cli[serve,duckdb,draft,draft-openai]>=1.12,<2
jafgen>=0.4.11
pre-commit>=3.6.0
requests~=2.31.0
typing-extensions>=4.12.2,<5
