# Direct dependencies for local development.
#
# Keep this file lightly pinned so pip can resolve compatible transitive
# dependencies across supported Python versions, including Python 3.13.
#
# datalex-cli is left without an upper bound on purpose: setup installs the
# latest DataLex CLI so the manifest is always built with current tooling.

dbt-duckdb>=1.10,<2.0
datalex-cli>=1.11
jafgen>=0.4.11
pre-commit>=3.6.0
requests~=2.31.0
typing-extensions>=4.12.2,<5
